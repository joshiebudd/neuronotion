const Index = ({ color }) => {
  return (
    <div className="flex items-center justify-center relative">
      <img src="/up_arrow.png" height={42} width={42}></img>
    </div>
  );
};

export default Index;
